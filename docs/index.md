# MPO Research Report

This site publishes the repository report on implementing Maximum a Posteriori Policy Optimization (MPO), plus per-folder documentation under `src/`.

## Contents

- `Full Report`: the main project report (mirrored from the root `README.md`).
- `Source Folders`: one page per implementation folder under `src/`.

## Local preview

```bash
pip install -r requirements/requirements_docs.txt
mkdocs serve
```
